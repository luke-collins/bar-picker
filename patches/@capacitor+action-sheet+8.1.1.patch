diff --git a/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/ActionSheetPlugin.swift b/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/ActionSheetPlugin.swift
index 638de78..9cddf6c 100644
--- a/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/ActionSheetPlugin.swift
+++ b/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/ActionSheetPlugin.swift
@@ -15,11 +15,30 @@ public class ActionSheetPlugin: CAPPlugin, CAPBridgedPlugin, UIAdaptivePresentat
     private let implementation = ActionSheet()
     private var currentCall: CAPPluginCall?
 
+    // capacitor-swift-pm 8.x's .swiftinterface incorrectly gates several
+    // CAPPluginCall/CAPBridgeProtocol APIs behind an experimental compiler
+    // flag not enabled in standard toolchains, even though they're genuinely
+    // present in the compiled binary (same root cause as the other patches
+    // in this repo). This reads the always-available JSArray-returning
+    // getArray(_:_:) overload and downcasts each element, instead of the
+    // gated generic getArray<T>(_:_:) overload.
+    private func getObjectArray(_ call: CAPPluginCall, _ key: String) -> [JSObject] {
+        call.getArray(key, []).compactMap { $0 as? JSObject }
+    }
+
+    // CAPBridgeProtocol.viewController is gated the same way but is
+    // @objc-exposed (confirmed via the framework's generated Objective-C
+    // header as a plain property), so KVC reads it directly, bypassing the
+    // broken Swift-side interface.
+    private var hostViewController: UIViewController? {
+        (bridge as? NSObject)?.value(forKey: "viewController") as? UIViewController
+    }
+
     @objc func showActions(_ call: CAPPluginCall) {
         let title = call.options["title"] as? String
         let message = call.options["message"] as? String
 
-        let options = call.getArray("options", JSObject.self) ?? []
+        let options = getObjectArray(call, "options")
         var alertActions = [UIAlertAction]()
         var hasCancellableButton = false
         for (index, option) in options.enumerated() {
@@ -45,7 +64,7 @@ public class ActionSheetPlugin: CAPPlugin, CAPBridgedPlugin, UIAdaptivePresentat
         DispatchQueue.main.async { [weak self] in
             if let alertController = self?.implementation.buildActionSheet(title: title, message: message, actions: alertActions) {
                 self?.setCenteredPopover(alertController)
-                self?.bridge?.viewController?.present(alertController, animated: true) {
+                self?.hostViewController?.present(alertController, animated: true) {
                     if !hasCancellableButton {
                         self?.setupCancelationListeners(alertController, call)
                     }
diff --git a/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/UrlCheckerPlugin.swift b/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/UrlCheckerPlugin.swift
new file mode 100644
index 0000000..a990904
--- /dev/null
+++ b/node_modules/@capacitor/action-sheet/ios/Sources/ActionSheetPlugin/UrlCheckerPlugin.swift
@@ -0,0 +1,46 @@
+import Foundation
+import Capacitor
+import UIKit
+
+// Minimal custom plugin exposing UIApplication.canOpenURL to JS, since none
+// of our installed Capacitor plugins provide this and iOS requires a native
+// check (canOpenURL is blocked for schemes not declared in
+// LSApplicationQueriesSchemes -- see Info.plist). Only uses CAPPluginCall
+// APIs confirmed never gated by capacitor-swift-pm's upstream
+// "$NonescapableTypes" packaging bug (see the patches/ directory for the
+// full explanation), so this custom plugin needs no interface workaround
+// of its own.
+//
+// This file lives inside the @capacitor/action-sheet package (rather than
+// the App target) so that `cap sync`'s plugin scanner -- which only scans
+// each *installed npm plugin's* own ios source directory for an
+// objc-exposed plugin class name to populate the generated
+// capacitor.config.json's packageClassList -- picks it up automatically.
+// A plugin class placed directly in the App target compiles and links
+// fine but is never actually registered with the JS bridge, since
+// packageClassList is the only thing CapacitorBridge.registerPlugins()
+// consults, and it's fully regenerated (not merged) on every `cap sync`.
+// Registering it via CAPBridgeViewController.bridge?.registerPluginInstance()
+// from a custom view controller subclass was the other option, but
+// `bridge` on CAPBridgeViewController is itself gated by the same
+// $NonescapableTypes bug and (unlike some other gated APIs used elsewhere
+// in this project) isn't @objc-exposed, so there's no KVC fallback for it.
+@objc(UrlCheckerPlugin)
+public class UrlCheckerPlugin: CAPPlugin, CAPBridgedPlugin {
+    public let identifier = "UrlCheckerPlugin"
+    public let jsName = "UrlChecker"
+    public let pluginMethods: [CAPPluginMethod] = [
+        CAPPluginMethod(name: "canOpenUrl", returnType: CAPPluginReturnPromise)
+    ]
+
+    @objc func canOpenUrl(_ call: CAPPluginCall) {
+        let urlString = call.getString("url", "")
+        guard !urlString.isEmpty, let url = URL(string: urlString) else {
+            call.resolve(["value": false])
+            return
+        }
+        DispatchQueue.main.async {
+            call.resolve(["value": UIApplication.shared.canOpenURL(url)])
+        }
+    }
+}
